# krateo-alert-troubleshooter — the HyperDX-alert → Autopilot-analysis handler. HyperDX posts a
# fired alert to http://krateo-alert-troubleshooter.krateo-system.svc:8080/webhook; the handler
# calls the Autopilot A2A agent and writes a TroubleshootingReport CR. Image pinned to :v0.2.17,
# a real tag published (from main@a0bfebf) after the arm64 build fix — verified multi-arch
# (linux/amd64 + linux/arm64) with `docker buildx imagetools inspect`. Bump this version tag
# alongside any future handler.py/hyperdx*.py change that needs to reach the cluster.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: krateo-alert-troubleshooter
  namespace: krateo-system
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: krateo-alert-troubleshooter
  namespace: krateo-system
rules:
  - apiGroups: ["observability.krateo.io"]
    resources: ["troubleshootingreports", "troubleshootingreports/status"]
    verbs: ["create", "get", "list", "patch", "update"]
  # the reconciler reads Alert CRs (desired), seeds defaults, and writes their status
  - apiGroups: ["observability.krateo.io"]
    resources: ["alerts", "alerts/status"]
    verbs: ["get", "list", "watch", "create", "patch", "update"]
  # the key-bootstrap Job (same SA) writes the hyperdx-api-token Secret
  - apiGroups: [""]
    resources: ["secrets"]
    verbs: ["create", "get", "patch", "update"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: krateo-alert-troubleshooter
  namespace: krateo-system
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: krateo-alert-troubleshooter
subjects:
  - kind: ServiceAccount
    name: krateo-alert-troubleshooter
    namespace: krateo-system
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: krateo-alert-troubleshooter
  namespace: krateo-system
  labels: { app: krateo-alert-troubleshooter }
spec:
  replicas: 1
  selector:
    matchLabels: { app: krateo-alert-troubleshooter }
  template:
    metadata:
      labels: { app: krateo-alert-troubleshooter }
    spec:
      serviceAccountName: krateo-alert-troubleshooter
      containers:
        - name: handler
          image: ghcr.io/krateo-agentiko/alert-troubleshooter:v0.2.17
          ports:
            - containerPort: 8080
          env:
            - name: NAMESPACE
              value: krateo-system
            - name: AUTOPILOT_A2A_URL
              value: http://krateo-autopilot.krateo-system.svc:8080/
            - name: REPORT_COOLDOWN
              value: "1800"     # dedup window (s): don't re-analyze the same alert within this
            # --- reconciler (Alert CRs -> HyperDX v2 external API, Bearer auth) ---
            - name: HYPERDX_API_URL
              value: http://krateo-clickstack-api.krateo-system.svc:8000
            - name: HYPERDX_ACCESS_KEY
              valueFrom: { secretKeyRef: { name: hyperdx-api-token, key: accessKey } }
            - name: RECONCILE_INTERVAL
              value: "60"
            # default Alert CRs seeded by the reconciler (not shipped as CRs — CRD-order safe)
            - name: DEFAULT_ALERTS_JSON
              value: '[{"name":"error-log-volume","displayName":"Error log volume","where":"SeverityText:error OR SeverityText:Error OR SeverityText:ERROR","interval":"5m","threshold":1,"thresholdType":"above","message":"Error-log volume is elevated — Autopilot will auto-triage the root cause."},{"name":"warning-log-volume","displayName":"Warning log volume","where":"SeverityText:warn OR SeverityText:warning OR SeverityText:WARN","interval":"15m","threshold":25,"thresholdType":"above","message":"Warning-log volume crossed the threshold — Autopilot will summarise likely causes."}]'
          readinessProbe:
            httpGet: { path: /healthz, port: 8080 }
            initialDelaySeconds: 3
          securityContext:
            allowPrivilegeEscalation: false
            runAsNonRoot: true
            capabilities: { drop: ["ALL"] }
---
apiVersion: v1
kind: Service
metadata:
  name: krateo-alert-troubleshooter
  namespace: krateo-system
spec:
  # Headless: this bench cluster's ClusterIP range is exhausted; a headless service still
  # resolves krateo-alert-troubleshooter.krateo-system.svc → the pod IP for HyperDX's webhook POST.
  clusterIP: None
  selector: { app: krateo-alert-troubleshooter }
  ports:
    - port: 8080
      targetPort: 8080
