{
  "$schema": "http://json-schema.org/draft-07/schema",
  "title": "Alert Troubleshooter",
  "description": "Bridges a HyperDX observability alert to a krateo-autopilot root-cause analysis, written back as a TroubleshootingReport CR the portal renders.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "global": {
      "type": "object",
      "additionalProperties": true,
      "description": "Global values shared across charts. Helm/core-provider inject this reserved key at install time, so a top-level additionalProperties:false schema MUST declare it or the composition's chart-inspector validation fails ('additional properties global not allowed')."
    },
    "image": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "repository": {
          "type": "string",
          "title": "Image repository",
          "default": "ghcr.io/krateo-platformops/alert-troubleshooter"
        },
        "tag": {
          "type": "string",
          "title": "Image tag",
          "description": "Container image tag. Empty defaults to the chart appVersion (= the release tag), so chart and image move together."
        }
      }
    },
    "config": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "autopilotA2aUrl": {
          "type": "string",
          "title": "Autopilot A2A URL",
          "description": "A2A endpoint of the krateo-autopilot agent the webhook calls for root-cause analysis. Defaults to the in-cluster kagent A2A Service `http://autopilot.krateo-system.svc:8080/` (kagent names the Service after the `autopilot` Agent, not `krateo-autopilot`).",
          "default": "http://autopilot.krateo-system.svc:8080/"
        },
        "hyperdxApiUrl": {
          "type": "string",
          "title": "HyperDX API URL",
          "description": "HyperDX v2 external API the reconciler pushes Alert CRs to (Bearer auth). Defaults to the krateo-clickstack-api Service.",
          "default": "http://krateo-clickstack-api.krateo-system.svc:8000"
        },
        "reportCooldown": {
          "type": "integer",
          "title": "Report cooldown (s)",
          "description": "Dedup window in seconds: don't re-analyze the same alert within this.",
          "default": 1800
        },
        "reconcileInterval": {
          "type": "integer",
          "title": "Reconcile interval (s)",
          "description": "How often (seconds) the reconciler pushes Alert CRs to HyperDX.",
          "default": 60
        },
        "defaultAlerts": {
          "type": "array",
          "title": "Default alerts",
          "description": "Default Alert CRs the reconciler seeds into HyperDX (rendered to DEFAULT_ALERTS_JSON). Set to [] to seed none.",
          "items": {
            "type": "object",
            "additionalProperties": true,
            "properties": {
              "name": { "type": "string" },
              "displayName": { "type": "string" },
              "where": { "type": "string" },
              "interval": { "type": "string" },
              "threshold": { "type": "number" },
              "thresholdType": { "type": "string" },
              "message": { "type": "string" }
            }
          }
        }
      }
    }
  }
}
